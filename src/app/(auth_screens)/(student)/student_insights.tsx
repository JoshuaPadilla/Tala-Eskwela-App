import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const StudentInsights = () => {
  return (
    <SafeAreaView>
      <Text>StudentInsights</Text>
    </SafeAreaView>
  );
};

export default StudentInsights;
