import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const TeacherInsights = () => {
  return (
    <SafeAreaView>
      <Text>TeacherInsights</Text>
    </SafeAreaView>
  );
};

export default TeacherInsights;
