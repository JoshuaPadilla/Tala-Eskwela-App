import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const TeacherSubjects = () => {
  return (
    <SafeAreaView>
      <Text>TeacherSubjects</Text>
    </SafeAreaView>
  );
};

export default TeacherSubjects;
